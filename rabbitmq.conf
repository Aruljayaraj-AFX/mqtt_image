# Enable MQTT plugin listener on port 1883 (default port, no need to specify)
mqtt.default_user = admin
mqtt.default_pass = supersecret123

# Optional: limit MQTT max connections
mqtt.max_connections = 1000

# Allow anonymous connections (set to false if you want to enforce authentication)
mqtt.allow_anonymous = false

# Log level for troubleshooting
log.console.level = info

# You can also enable MQTT over WebSockets if needed (port 8083)
# mqtt.listeners.ws.default = 8083
